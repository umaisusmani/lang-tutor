# Groq — LLM for the conversation + corrections (free tier: console.groq.com)
GROQ_API_KEY=

# Google Gemini — embeddings for the grammar-rule RAG corpus (added in step 5)
# GOOGLE_GENERATIVE_AI_API_KEY=
